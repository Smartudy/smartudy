<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorMain</item>
        <item name="toolbarStyle">@style/myToolbarStyle</item>
        <!--Status Bar 글자색깔
        <item name="android:statusBarColor">#ffffff</item>
        -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="myToolbarStyle" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:paddingRight">10dp</item>
        <item name="android:background">@color/colorBackGround</item>
        <item name="colorControlNormal">#000000</item>
        <item name="android:fontFamily">@font/nanumsquareb</item>
    </style>

    <!-- QnA창 스타일 -->
    <style name="QnA_title_style">
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@string/main_font</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="QnA_toolbar_style" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">#ffffff</item>
    </style>

    <!-- 글쓰기창 스타일 -->
    <style name="toolbar_write_title_style">
        <item name="android:textSize">16dp</item>
        <item name="android:fontFamily">@string/main_font</item>
        <item name="android:textColor">@color/colorTitle</item>
    </style>

    <style name="contents_write_style">
        <item name="android:textSize">14dp</item>
        <item name="android:fontFamily">@string/sub_r_font</item>
        <item name="android:textColor">@color/colorTitle</item>
    </style>

    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@color/colorBackGround</item>
        <item name="colorControlNormal">#000000</item>
        <item name="android:fontFamily">@font/nanumsquareb</item>
    </style>

    <style name="bottom_write_style">
        <item name="android:textSize">10dp</item>
        <item name="android:fontFamily">@string/sub_r_font</item>
        <item name="android:textColor">@color/colorTitle</item>
    </style>
    
    <style name="myDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
